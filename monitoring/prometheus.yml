# Prometheus scrape configuration for Agent Saan
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "agent_saan_api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
